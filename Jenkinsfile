pipeline {
      agent any
      stages {
           stage ('BUILD'){
              steps {
                    echo "BUILDING"
                    echo "BUILD "
                 }
             }
           stage ('TEST'){
                 steps {
                    echo "TESTING"
                 }
            }
           stage ('DEPLOY'){
                  steps {
                    echo "DEPLOYING"
                 }
             }
       }
}
