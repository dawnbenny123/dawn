pipeline {
      agent any
      stages {
           stage ('BUILD'){
              steps {
                    echo "BUILDING the application"
                 }
             }
           stage ('TEST'){
                 steps {
                    echo "TESTING"
                 }
            }
           stage ('DEPLOY'){
                  steps {
                    echo "DEPLOYING"
                 }
             }
       }
}
